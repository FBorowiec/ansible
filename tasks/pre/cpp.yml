---
- name: Install cpp packages
  ansible.builtin.apt:
    name: "{{ item }}"
  loop:
    - autoconf
    - automake
    - clang
    - clang-format
    - clang-tidy
    - clangd
    - cmake
    - meson
    - gcc
    - gdb
    - g++
    - libboost-all-dev
